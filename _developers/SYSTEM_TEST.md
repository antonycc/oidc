Please also get this test working: ` npm run test:web` Keep going until that passes and also have another look at the API test and actually see if you can request the expected grant (look at the endpoints to decide this and if they are broken fix them so we can re-deploy to AWS. Also set up an express sever under ./app/bin that will expose all the endpoints and test them with a similar test from a new set of system tests under ./app/system. Have the same flow tested in there from ./authorize -> ./token -> ./userinfo but running against an express server and using vitest (no playright), and create a script section in package.json for the system tests. Also create a unit test (vitest) that can test a click around the ./web pages without running playwright and put that in the system test directory too. Update package.json so that `npm test` runs `npm run test:unit` and `npm run test:system`. Keep working until `npm test passes`